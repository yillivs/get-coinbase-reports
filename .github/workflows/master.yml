name: Python package

on:
  push:
    branches: 
      - lambdafy-the-code
  pull_request:
    branches: 
      - lambdafy-the-code

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Activate virtual environment and install dependencies
      run: |
        cd get-coinbase-reports
        python -m venv venv
        source venv/bin/activate
        pip install -r requirements.txt
        pip show boto3
        deactivate
    - name: Zip deployment package
      run: |
        ls
        cd get-coinbase-reports/venv/lib/python3.9/site-packages
        zip -r ../../../../my_deployment_package.zip .
        cd ../../../../
        zip my_deployment_package.zip lambda_function.py
#        zip -r lambda_function.zip . -x "venv/*"
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: "us-east-1"
    - name: Upload deployment package to S3
      run: |
        ls get-coinbase-reports
        aws s3 cp get-coinbase-reports/my_deployment_package.zip s3://coinbase-report/lambda_function.zip
#    - name: Deploy to AWS
#      run: |
#        aws lambda update-function-code --function-name coinbase-brassert-report --zip-file fileb://lambda_function.zip


    
   
